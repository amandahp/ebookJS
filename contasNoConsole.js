/**Operadores aritméticos:
 * + -> adição
 * - -> subtração
 * / -> Divisão
 * * -> Multiplicação
 * % -> Módulo (pega o resto da divisão)
 * ** -> exponenciação
 */

var x = 4;
alert(y = x + 4, "Y é igual a: " + y);

/**Operadores relacionais:
  * == -> verifica a igualdade entre dois valores,
  * != -> verifica a diferença entre os dois valores
  * > -> verifica se um valor é maior
  * < -> verifica se um valor é menor
  * >= -> verifica se um valor é maior ou igual a outro
  * <= -> verifica se um valor é menor ou igual a outro
  */

var k = 8;
alert(k==7);
